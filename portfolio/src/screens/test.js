/* {graphic.map((item, index) => {
    return (
      <span className="img-fluid">
        {item.photos.map((c, i) => (
          <img src={c.photo} alt="project" className="img-fluid" />
        ))}
      </span>
    );
  })} */
